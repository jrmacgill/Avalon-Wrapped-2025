export const config = {
  year: 2025,
  dataDir: 'G:\\Discord\\ReScrape', // Centralizing data directory as well since it's used in multiple places
  targetUser: 'Gemtun Underleaf' // Centralizing target user for pun analysis
}
